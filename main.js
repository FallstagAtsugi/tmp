const name = "柿崎";
const age = 26;
console.log(`私の名前は${name}です。年齢は${age}です。`);

// function add(a, b) {
//     return a - b;
// }

//アロー関数
const add = (a, b) => {
  return a + b;
};

console.log(add(1, 4));
